import { TimePublishedPipe } from './time-published.pipe';

describe('TimePublishedPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePublishedPipe();
    expect(pipe).toBeTruthy();
  });
});
