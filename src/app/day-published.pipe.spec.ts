import { DayPublishedPipe } from './day-published.pipe';

describe('DayPublishedPipe', () => {
  it('create an instance', () => {
    const pipe = new DayPublishedPipe();
    expect(pipe).toBeTruthy();
  });
});
